#!/bin/bash

apt update
apt install sudo -y # in case already in root
sudo apt install -y zsh git curl vim wget tmux pipx

if [[ $1 == "--cn" ]]
then
    echo "Will use cn proxy."
    export cnproxy=https://mirror.ghproxy.com/
else
    unset cnproxy
fi

./install-base